# Environment
NODE_ENV=development
PORT=3000

# Database
MONGODB_URI=mongodb://localhost:27017/turnos-pro

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Setup (para crear primer admin - one-time use)
# Cambia a false después de crear el primer admin para deshabilitar permanentemente
SETUP_ENABLED=true
SETUP_TOKEN=changeme-strong-token-for-admin-setup

# CORS
CORS_ORIGIN=http://localhost:4200
# Kiosco URL (opcional, para CORS del kiosco Astro)
KIOSK_URL=http://localhost:3000

# Kiosk API Key (opcional): clave única para el kiosk Astro. Estática, no vence.
# Si está definida, las peticiones a /queue/kiosk/* deben enviar header X-Kiosk-Key.
# Configura la misma clave en el kiosk como PUBLIC_KIOSK_API_KEY.
# KIOSK_API_KEY=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Modo demo (opcional)
# Si DEMO_MODE=true: no se pueden crear nuevos usuarios ni clientes; solo los IDs indicados.
# Crea manualmente un usuario recepcionista y un cliente, luego pon sus IDs aquí.
# Un cron borra diariamente los tickets/colas generados en modo demo.
# DEMO_MODE=false
# DEMO_USER_ID=
# DEMO_CLIENT_ID=
